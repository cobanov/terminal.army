# Screenshots referenced by README.md

Replace the placeholder PNGs in this directory with your own captures:

- `hero.png`        — the banner shown at the top of the README
- `signup.png`      — the install + signup landing on terminal.army
- `dashboard.png`   — main TUI screen (planet card + log + right panel)
- `galaxy.png`      — the /galaxy modal with arrow-key hints visible
- `research.png`    — /research with the tech tree + costs
- `commands.png`    — a couple of command outputs side by side

Filenames matter — README references each by name.
